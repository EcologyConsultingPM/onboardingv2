# Role Entry Verification

The portal root now opens a dedicated **Choose your workspace** screen. It presents two compact, rectangular, colour-coded options read left to right: **Staff Portal** and **Admin Portal**. Each option uses an inline icon, title, supporting description, and directional cue.

The Admin option remains protected by the authenticated account role. A staff account attempting to enter it receives an access message rather than being granted administrator rights. The desktop browser preview displayed the signed-in team member, both role options, the Ecology Consulting visual identity, and the role-control notice correctly.

The selected Staff or Admin choice is stored through the authentication hand-off and, after sign-in, the user is sent to the workspace permitted by their account role. The portal was restarted after the router update; its root entry screen loaded successfully in the browser, and the current router defines the compliance status validation set before it is used by the WHS compliance procedures.

The entry screen was also verified at a 390 × 844 mobile viewport. The Staff and Admin rectangular options stack in a clear top-to-bottom sequence, retain large tap targets and left-to-right content within each option, and keep the role-control explanation visible without horizontal overflow.
